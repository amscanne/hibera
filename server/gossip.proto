package server;

enum TYPE {
    PING = 0;
    PONG = 1;
};

message Message {
    required uint32 type = 1;
    required string version = 2;
    required string id = 3;
    repeated string dead = 4;
}
